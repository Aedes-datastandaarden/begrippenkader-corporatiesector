---
import Layout from '../layouts/Layout.astro';
import HomeContent from '../components/HomeContent';
import versions from '../../public/build/versions.json';
import scheme from '../../public/build/latest/scheme.json';
import collections from '../../public/build/latest/collections.json';
import allConcepts from '../../public/build/latest/concepts.json';
---

<Layout title={scheme.title} description={scheme.description}>
  <HomeContent
    client:load
    defaultScheme={scheme}
    defaultCollections={collections}
    defaultConcepts={allConcepts}
    versionsManifest={versions}
  />
</Layout>
